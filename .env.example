# Copy to .env and fill in. Never commit the real .env.
# Hosted Postgres connection string (Neon / Supabase / Render / Railway).
# The app self-creates tables and seeds demo data on first /api/health call.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
